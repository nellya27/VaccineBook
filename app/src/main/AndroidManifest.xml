<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="msd.com.vaccinebook">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserLogin"
            android:label="@string/title_activity_user_login"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DoctorLogin"
            android:label="@string/title_activity_doctorlogin2"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SignUser"
            android:label="@string/title_activity_sign_user"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".UserInterface"
            android:label="@string/title_activity_user_interface"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DoctorInterface"
            android:label="@string/title_activity_doctor_interface"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".welcomeUser"
            android:label="@string/title_activity_welcome_user"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".WelcomeUserActivity"
            android:label="@string/title_activity_view_options"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PatientVaccineListActivity"
            android:label="@string/title_activity_patient_vaccine_list"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".VaccineInfo"
            android:label="@string/title_activity_vaccine_info"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".vaccineForDoctor"
            android:label="@string/title_activity_vaccine_for_doctor"
            android:theme="@style/AppTheme">
        </activity>
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".AddVaccineActivity"
            android:label="@string/title_activity_add_vaccine"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".ChooseVaccineToAddActivity"
            android:label="@string/title_activity_choose_vaccine_to_add"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ShowPatientVaccinesListActivity"
            android:label="@string/title_activity_show_patient_vaccines_list"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ChooseVaccineToAddFromExpandableAdapterActivity"
            android:label="@string/title_activity_choose_vaccine_to_add_from_expandable_adapter"
            android:theme="@style/AppTheme" />


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>